# 🚨 修正 item_id 模糊錯誤 (42702)

## 問題
前端 Console 出現錯誤：
```
code: '42702'
message: 'column reference "item_id" is ambiguous'
```

## 解決方法（只需 3 步驟）

### 步驟 1️⃣: 開啟 Supabase Dashboard
1. 前往 https://supabase.com/dashboard
2. 選擇您的專案
3. 點擊左側選單的 **SQL Editor**

### 步驟 2️⃣: 執行修正腳本
1. 在 SQL Editor 點擊 **New query**
2. 開啟專案中的檔案: `backend/COPY_TO_SUPABASE_DASHBOARD.sql`
3. **複製全部內容** 並貼到 SQL Editor
4. 點擊 **Run** (或按 `Ctrl+Enter` / `Cmd+Enter`)
5. 等待執行完成，應該會看到成功訊息

### 步驟 3️⃣: 測試修正
1. **清除瀏覽器快取**
   - Chrome/Edge: 按 `Ctrl+Shift+Delete` (Mac: `Cmd+Shift+Delete`)
   - 勾選「快取」和「Cookie」
   - 點擊「清除資料」
   - **或者直接使用無痕視窗測試**

2. **重新整理應用程式**

3. **登入並測試**
   - 瀏覽任一物品頁面
   - 點擊「聯絡賣家」按鈕
   - 檢查瀏覽器 Console

## ✅ 成功指標
- Console 不再出現 `42702` 錯誤
- Console 不再出現 "ambiguous" 訊息
- 可以成功發起對話
- 重複點擊會返回相同對話

## ❓ 如果還是失敗

### 檢查 1: 確認函數已更新
在 SQL Editor 執行：
```sql
SELECT pg_get_function_result(p.oid)
FROM pg_proc p
WHERE p.proname = 'create_or_get_conversation'
  AND p.pronamespace = 'public'::regnamespace;
```

應該看到返回類型包含 `OUT conversation_id bigint, OUT item_id bigint...`

### 檢查 2: 清除快取要徹底
- 關閉所有瀏覽器視窗
- 重新開啟
- 或使用無痕視窗

### 檢查 3: 查看完整錯誤
在瀏覽器 Console:
- 展開錯誤物件
- 檢查 `error.code`、`error.message`、`error.details`
- 截圖提供給開發團隊

## 📚 技術細節

**問題根源**: 
PostgreSQL 的 `RETURNS TABLE` 會將欄位名稱 (如 `item_id`) 引入函數作用域，
導致與 SQL 查詢中的資料表欄位產生名稱衝突。

**解決方案**: 
使用 `OUT` 參數代替 `RETURNS TABLE`，完全避免名稱衝突。

**相容性**: 
前端呼叫方式完全不變，Supabase RPC 完全支援 OUT 參數。

---

**預計修正時間**: < 5 分鐘  
**影響範圍**: 僅限「聯絡賣家」功能  
**資料安全**: ✅ 不影響既有資料