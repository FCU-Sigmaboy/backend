# 用戶通訊模組 - 角色互換改造方案 v2 (平滑過渡版本)

## 📋 文檔資訊

- **文檔版本**: 2.0.0
- **建立日期**: 2024-01-XX
- **最後更新**: 2024-01-XX
- **狀態**: 規劃中
- **作者**: Development Team

---

## 🎯 設計目標

本方案旨在**不破壞現有 v1 系統**的前提下,建立全新的 v2 版本通訊模組,實現:

1. ✅ **向後相容**: v1 系統繼續正常運作
2. ✅ **平滑過渡**: 開發者可以逐步遷移到 v2
3. ✅ **角色互換**: 支援買賣雙方角色動態切換
4. ✅ **功能增強**: 多商品對話、更靈活的訊息管理
5. ✅ **數據遷移**: 提供 v1 到 v2 的數據遷移工具

---

## 🏗️ 架構設計概覽

### 雙版本並存架構

```
Current (v1)                    New (v2)
┌─────────────────┐            ┌─────────────────┐
│  conversations  │            │ conversations_v2│
│  (保持不變)      │            │  (新表)         │
└─────────────────┘            └─────────────────┘
        ↓                              ↓
┌─────────────────┐            ┌─────────────────┐
│conversation_    │            │conversation_    │
│messages         │            │messages_v2      │
│  (保持不變)      │            │  (新表)         │
└─────────────────┘            └─────────────────┘
        ↓                              ↓
┌─────────────────┐            ┌─────────────────┐
│  v1 RPC         │            │  v2 RPC         │
│  functions      │            │  functions      │
└─────────────────┘            └─────────────────┘
```

### 命名規範

- **資料表**: 新表加上 `_v2` 後綴 (如 `conversations_v2`)
- **函數**: 新函數加上 `_v2` 後綴 (如 `create_or_get_conversation_v2`)
- **索引**: 新索引加上 `_v2` 後綴
- **RLS 政策**: 新政策加上 `_v2` 後綴

---

## 📐 資料表結構設計

### 1. `conversations_v2` (對話表 v2)

#### 核心設計改變

**v1 問題**:
- 固定 `buyer_id` 和 `seller_id` 角色
- 一個對話只能討論一個商品
- 角色無法互換

**v2 解決方案**:
- 使用 `participant_1_id` 和 `participant_2_id` (去角色化)
- 支援多商品討論
- 通過 `conversation_participants_v2` 表管理參與者角色

#### 資料表結構

```sql
-- ============================================
-- 對話表 v2 (去角色化設計)
-- ============================================
CREATE TABLE IF NOT EXISTS public.conversations_v2 (
    id BIGSERIAL PRIMARY KEY,

    -- 參與者 (去角色化)
    participant_1_id UUID NOT NULL,
    participant_2_id UUID NOT NULL,

    -- 對話元數據
    initial_item_id BIGINT,  -- 初始商品 (可為 NULL,支援純聊天)

    -- 狀態管理
    is_active BOOLEAN NOT NULL DEFAULT true,
    archived_by_participant_1 BOOLEAN NOT NULL DEFAULT false,
    archived_by_participant_2 BOOLEAN NOT NULL DEFAULT false,

    -- 時間戳記
    created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    last_message_at TIMESTAMPTZ,

    -- 外鍵約束
    FOREIGN KEY (participant_1_id) REFERENCES public.users(id) ON DELETE CASCADE,
    FOREIGN KEY (participant_2_id) REFERENCES public.users(id) ON DELETE CASCADE,
    FOREIGN KEY (initial_item_id) REFERENCES public.items(id) ON DELETE SET NULL,

    -- 唯一約束 (確保兩個用戶只有一個對話,不論誰先誰後)
    CONSTRAINT conversations_v2_participants_unique
        UNIQUE (participant_1_id, participant_2_id),

    -- 檢查約束 (防止自己跟自己對話)
    CONSTRAINT conversations_v2_different_participants
        CHECK (participant_1_id != participant_2_id),

    -- 檢查約束 (確保 participant_1_id < participant_2_id,標準化排序)
    CONSTRAINT conversations_v2_participant_order
        CHECK (participant_1_id < participant_2_id)
);

-- 索引優化
CREATE INDEX idx_conversations_v2_participant_1
    ON public.conversations_v2(participant_1_id, updated_at DESC);

CREATE INDEX idx_conversations_v2_participant_2
    ON public.conversations_v2(participant_2_id, updated_at DESC);

CREATE INDEX idx_conversations_v2_initial_item
    ON public.conversations_v2(initial_item_id)
    WHERE initial_item_id IS NOT NULL;

CREATE INDEX idx_conversations_v2_last_message
    ON public.conversations_v2(last_message_at DESC NULLS LAST);

CREATE INDEX idx_conversations_v2_active
    ON public.conversations_v2(is_active)
    WHERE is_active = true;

-- 自動更新 updated_at
CREATE TRIGGER update_conversations_v2_updated_at
    BEFORE UPDATE ON public.conversations_v2
    FOR EACH ROW
    EXECUTE FUNCTION public.update_updated_at_column();

-- 註解
COMMENT ON TABLE public.conversations_v2 IS 'v2版本對話表,支援去角色化設計和多商品討論';
COMMENT ON COLUMN public.conversations_v2.participant_1_id IS '參與者1 (UUID較小者)';
COMMENT ON COLUMN public.conversations_v2.participant_2_id IS '參與者2 (UUID較大者)';
COMMENT ON COLUMN public.conversations_v2.initial_item_id IS '對話起始商品,可為NULL';
COMMENT ON COLUMN public.conversations_v2.archived_by_participant_1 IS '參與者1是否歸檔此對話';
COMMENT ON COLUMN public.conversations_v2.archived_by_participant_2 IS '參與者2是否歸檔此對話';
```

### 2. `conversation_messages_v2` (訊息表 v2)

```sql
-- ============================================
-- 訊息表 v2 (增強版)
-- ============================================
CREATE TABLE IF NOT EXISTS public.conversation_messages_v2 (
    id BIGSERIAL PRIMARY KEY,
    conversation_id BIGINT NOT NULL,
    sender_id UUID NOT NULL,

    -- 訊息內容
    content TEXT NOT NULL,
    message_type VARCHAR(20) NOT NULL DEFAULT 'text',

    -- 商品引用 (新功能)
    related_item_id BIGINT,  -- 此訊息討論的商品

    -- 訊息狀態
    is_deleted BOOLEAN NOT NULL DEFAULT false,
    deleted_at TIMESTAMPTZ,

    -- 已讀狀態
    read_by_participant_1 BOOLEAN NOT NULL DEFAULT false,
    read_by_participant_2 BOOLEAN NOT NULL DEFAULT false,
    read_at_participant_1 TIMESTAMPTZ,
    read_at_participant_2 TIMESTAMPTZ,

    -- 時間戳記
    created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),

    -- 外鍵約束
    FOREIGN KEY (conversation_id) REFERENCES public.conversations_v2(id) ON DELETE CASCADE,
    FOREIGN KEY (sender_id) REFERENCES public.users(id) ON DELETE CASCADE,
    FOREIGN KEY (related_item_id) REFERENCES public.items(id) ON DELETE SET NULL,

    -- 檢查約束
    CONSTRAINT conversation_messages_v2_valid_type
        CHECK (message_type IN ('text', 'image', 'system', 'item_reference'))
);

-- 索引優化
CREATE INDEX idx_conversation_messages_v2_conversation
    ON public.conversation_messages_v2(conversation_id, created_at DESC);

CREATE INDEX idx_conversation_messages_v2_sender
    ON public.conversation_messages_v2(sender_id, created_at DESC);

CREATE INDEX idx_conversation_messages_v2_related_item
    ON public.conversation_messages_v2(related_item_id)
    WHERE related_item_id IS NOT NULL;

CREATE INDEX idx_conversation_messages_v2_unread
    ON public.conversation_messages_v2(conversation_id, sender_id)
    WHERE NOT is_deleted;

-- 自動更新 updated_at
CREATE TRIGGER update_conversation_messages_v2_updated_at
    BEFORE UPDATE ON public.conversation_messages_v2
    FOR EACH ROW
    EXECUTE FUNCTION public.update_updated_at_column();

-- 觸發器:更新對話的 last_message_at
CREATE OR REPLACE FUNCTION update_conversation_v2_last_message()
RETURNS TRIGGER AS $$
BEGIN
    UPDATE public.conversations_v2
    SET
        last_message_at = NEW.created_at,
        updated_at = now()
    WHERE id = NEW.conversation_id;
    RETURN NEW;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;

CREATE TRIGGER trigger_update_conversation_v2_last_message
    AFTER INSERT ON public.conversation_messages_v2
    FOR EACH ROW
    EXECUTE FUNCTION update_conversation_v2_last_message();

-- 註解
COMMENT ON TABLE public.conversation_messages_v2 IS 'v2版本訊息表,支援商品引用和增強的已讀狀態';
COMMENT ON COLUMN public.conversation_messages_v2.related_item_id IS '此訊息討論的商品ID';
COMMENT ON COLUMN public.conversation_messages_v2.read_by_participant_1 IS '參與者1是否已讀';
COMMENT ON COLUMN public.conversation_messages_v2.read_by_participant_2 IS '參與者2是否已讀';
```

### 3. `conversation_items_v2` (對話商品關聯表)

```sql
-- ============================================
-- 對話商品關聯表 (追蹤對話中討論過的所有商品)
-- ============================================
CREATE TABLE IF NOT EXISTS public.conversation_items_v2 (
    id BIGSERIAL PRIMARY KEY,
    conversation_id BIGINT NOT NULL,
    item_id BIGINT NOT NULL,

    -- 商品加入對話的方式
    added_by_user_id UUID NOT NULL,
    added_via_message_id BIGINT,  -- 通過哪條訊息加入

    -- 狀態
    is_active BOOLEAN NOT NULL DEFAULT true,

    -- 時間戳記
    created_at TIMESTAMPTZ NOT NULL DEFAULT now(),

    -- 外鍵約束
    FOREIGN KEY (conversation_id) REFERENCES public.conversations_v2(id) ON DELETE CASCADE,
    FOREIGN KEY (item_id) REFERENCES public.items(id) ON DELETE CASCADE,
    FOREIGN KEY (added_by_user_id) REFERENCES public.users(id) ON DELETE CASCADE,
    FOREIGN KEY (added_via_message_id) REFERENCES public.conversation_messages_v2(id) ON DELETE SET NULL,

    -- 唯一約束 (同一對話中同一商品只記錄一次)
    UNIQUE(conversation_id, item_id)
);

-- 索引
CREATE INDEX idx_conversation_items_v2_conversation
    ON public.conversation_items_v2(conversation_id, created_at DESC);

CREATE INDEX idx_conversation_items_v2_item
    ON public.conversation_items_v2(item_id);

-- 註解
COMMENT ON TABLE public.conversation_items_v2 IS '追蹤對話中討論過的所有商品';
```

---

## 🔐 RLS 安全性策略

### `conversations_v2` 表 RLS

```sql
-- 啟用 RLS
ALTER TABLE public.conversations_v2 ENABLE ROW LEVEL SECURITY;

-- 政策 1: 查詢權限 (參與者可以查詢)
CREATE POLICY conversations_v2_select_policy ON public.conversations_v2
    FOR SELECT
    TO authenticated
    USING (
        auth.uid() = participant_1_id
        OR auth.uid() = participant_2_id
    );

-- 政策 2: 插入權限 (用戶可以建立對話,但需通過 RPC)
CREATE POLICY conversations_v2_insert_policy ON public.conversations_v2
    FOR INSERT
    TO authenticated
    WITH CHECK (
        auth.uid() IN (participant_1_id, participant_2_id)
    );

-- 政策 3: 更新權限 (參與者可以更新歸檔狀態)
CREATE POLICY conversations_v2_update_policy ON public.conversations_v2
    FOR UPDATE
    TO authenticated
    USING (
        auth.uid() = participant_1_id
        OR auth.uid() = participant_2_id
    )
    WITH CHECK (
        auth.uid() = participant_1_id
        OR auth.uid() = participant_2_id
    );

-- 政策 4: 刪除權限 (不允許直接刪除,只能歸檔)
CREATE POLICY conversations_v2_delete_policy ON public.conversations_v2
    FOR DELETE
    TO authenticated
    USING (false);  -- 禁止刪除
```

### `conversation_messages_v2` 表 RLS

```sql
-- 啟用 RLS
ALTER TABLE public.conversation_messages_v2 ENABLE ROW LEVEL SECURITY;

-- 政策 1: 查詢權限 (對話參與者可以查詢)
CREATE POLICY conversation_messages_v2_select_policy ON public.conversation_messages_v2
    FOR SELECT
    TO authenticated
    USING (
        EXISTS (
            SELECT 1 FROM public.conversations_v2 c
            WHERE c.id = conversation_id
            AND (c.participant_1_id = auth.uid() OR c.participant_2_id = auth.uid())
        )
    );

-- 政策 2: 插入權限 (對話參與者可以發送訊息)
CREATE POLICY conversation_messages_v2_insert_policy ON public.conversation_messages_v2
    FOR INSERT
    TO authenticated
    WITH CHECK (
        auth.uid() = sender_id
        AND EXISTS (
            SELECT 1 FROM public.conversations_v2 c
            WHERE c.id = conversation_id
            AND (c.participant_1_id = auth.uid() OR c.participant_2_id = auth.uid())
        )
    );

-- 政策 3: 更新權限 (只能更新已讀狀態和刪除狀態)
CREATE POLICY conversation_messages_v2_update_policy ON public.conversation_messages_v2
    FOR UPDATE
    TO authenticated
    USING (
        EXISTS (
            SELECT 1 FROM public.conversations_v2 c
            WHERE c.id = conversation_id
            AND (c.participant_1_id = auth.uid() OR c.participant_2_id = auth.uid())
        )
    );

-- 政策 4: 刪除權限 (禁止直接刪除,使用軟刪除)
CREATE POLICY conversation_messages_v2_delete_policy ON public.conversation_messages_v2
    FOR DELETE
    TO authenticated
    USING (false);
```

### `conversation_items_v2` 表 RLS

```sql
-- 啟用 RLS
ALTER TABLE public.conversation_items_v2 ENABLE ROW LEVEL SECURITY;

-- 政策: 查詢權限 (對話參與者可以查詢)
CREATE POLICY conversation_items_v2_select_policy ON public.conversation_items_v2
    FOR SELECT
    TO authenticated
    USING (
        EXISTS (
            SELECT 1 FROM public.conversations_v2 c
            WHERE c.id = conversation_id
            AND (c.participant_1_id = auth.uid() OR c.participant_2_id = auth.uid())
        )
    );

-- 插入和更新通過 RPC 函數處理
```

---

## 📦 核心 RPC 函數

### 1. 輔助函數:標準化參與者順序

```sql
-- ============================================
-- 輔助函數:確保 participant_1 < participant_2
-- ============================================
CREATE OR REPLACE FUNCTION normalize_participants_v2(
    p_user_a UUID,
    p_user_b UUID
)
RETURNS TABLE(participant_1 UUID, participant_2 UUID)
LANGUAGE plpgsql
SECURITY DEFINER
AS $$
BEGIN
    IF p_user_a < p_user_b THEN
        RETURN QUERY SELECT p_user_a, p_user_b;
    ELSE
        RETURN QUERY SELECT p_user_b, p_user_a;
    END IF;
END;
$$;

COMMENT ON FUNCTION normalize_participants_v2 IS 'v2: 標準化參與者順序,確保 participant_1 < participant_2';
```

### 2. 建立或取得對話 v2

```sql
-- ============================================
-- 建立或取得對話 v2
-- ============================================
CREATE OR REPLACE FUNCTION create_or_get_conversation_v2(
    p_other_user_id UUID,
    p_initial_item_id BIGINT DEFAULT NULL
)
RETURNS TABLE(
    conversation_id BIGINT,
    participant_1_id UUID,
    participant_2_id UUID,
    initial_item_id BIGINT,
    is_new BOOLEAN,
    current_user_is_participant_1 BOOLEAN
)
LANGUAGE plpgsql
SECURITY DEFINER
AS $$
DECLARE
    v_current_user_id UUID;
    v_participant_1 UUID;
    v_participant_2 UUID;
    v_conversation_id BIGINT;
    v_is_new BOOLEAN := false;
    v_user_is_p1 BOOLEAN;
BEGIN
    -- 取得當前用戶
    v_current_user_id := auth.uid();

    IF v_current_user_id IS NULL THEN
        RAISE EXCEPTION 'Not authenticated';
    END IF;

    -- 防止自己跟自己對話
    IF v_current_user_id = p_other_user_id THEN
        RAISE EXCEPTION 'Cannot create conversation with yourself';
    END IF;

    -- 標準化參與者順序
    SELECT p1, p2 INTO v_participant_1, v_participant_2
    FROM normalize_participants_v2(v_current_user_id, p_other_user_id) AS t(p1, p2);

    -- 判斷當前用戶是 participant_1 還是 participant_2
    v_user_is_p1 := (v_current_user_id = v_participant_1);

    -- 嘗試取得現有對話
    SELECT id INTO v_conversation_id
    FROM public.conversations_v2
    WHERE participant_1_id = v_participant_1
      AND participant_2_id = v_participant_2
    LIMIT 1;

    -- 如果對話不存在,建立新對話
    IF v_conversation_id IS NULL THEN
        INSERT INTO public.conversations_v2 (
            participant_1_id,
            participant_2_id,
            initial_item_id
        ) VALUES (
            v_participant_1,
            v_participant_2,
            p_initial_item_id
        )
        RETURNING id INTO v_conversation_id;

        v_is_new := true;

        -- 如果有初始商品,加入到 conversation_items_v2
        IF p_initial_item_id IS NOT NULL THEN
            INSERT INTO public.conversation_items_v2 (
                conversation_id,
                item_id,
                added_by_user_id
            ) VALUES (
                v_conversation_id,
                p_initial_item_id,
                v_current_user_id
            )
            ON CONFLICT (conversation_id, item_id) DO NOTHING;
        END IF;
    ELSE
        -- 對話已存在,但如果提供了新的商品,也要加入
        IF p_initial_item_id IS NOT NULL THEN
            INSERT INTO public.conversation_items_v2 (
                conversation_id,
                item_id,
                added_by_user_id
            ) VALUES (
                v_conversation_id,
                p_initial_item_id,
                v_current_user_id
            )
            ON CONFLICT (conversation_id, item_id) DO NOTHING;
        END IF;
    END IF;

    -- 返回結果
    RETURN QUERY
    SELECT
        v_conversation_id,
        v_participant_1,
        v_participant_2,
        p_initial_item_id,
        v_is_new,
        v_user_is_p1;
END;
$$;

COMMENT ON FUNCTION create_or_get_conversation_v2 IS 'v2: 建立或取得兩個用戶之間的對話,支援去角色化和多商品';
```

### 3. 發送訊息 v2

```sql
-- ============================================
-- 發送訊息 v2
-- ============================================
CREATE OR REPLACE FUNCTION send_message_v2(
    p_conversation_id BIGINT,
    p_content TEXT,
    p_message_type VARCHAR(20) DEFAULT 'text',
    p_related_item_id BIGINT DEFAULT NULL
)
RETURNS TABLE(
    message_id BIGINT,
    conversation_id BIGINT,
    sender_id UUID,
    content TEXT,
    message_type VARCHAR,
    related_item_id BIGINT,
    created_at TIMESTAMPTZ
)
LANGUAGE plpgsql
SECURITY DEFINER
AS $$
DECLARE
    v_current_user_id UUID;
    v_message_id BIGINT;
    v_participant_1 UUID;
    v_participant_2 UUID;
    v_user_is_p1 BOOLEAN;
BEGIN
    -- 取得當前用戶
    v_current_user_id := auth.uid();

    IF v_current_user_id IS NULL THEN
        RAISE EXCEPTION 'Not authenticated';
    END IF;

    -- 驗證用戶是對話參與者
    SELECT c.participant_1_id, c.participant_2_id
    INTO v_participant_1, v_participant_2
    FROM public.conversations_v2 c
    WHERE c.id = p_conversation_id;

    IF v_participant_1 IS NULL THEN
        RAISE EXCEPTION 'Conversation not found';
    END IF;

    IF v_current_user_id != v_participant_1 AND v_current_user_id != v_participant_2 THEN
        RAISE EXCEPTION 'Not a participant of this conversation';
    END IF;

    -- 判斷當前用戶是哪個參與者
    v_user_is_p1 := (v_current_user_id = v_participant_1);

    -- 插入訊息 (已讀狀態:發送者自己標記為已讀)
    INSERT INTO public.conversation_messages_v2 (
        conversation_id,
        sender_id,
        content,
        message_type,
        related_item_id,
        read_by_participant_1,
        read_by_participant_2,
        read_at_participant_1,
        read_at_participant_2
    ) VALUES (
        p_conversation_id,
        v_current_user_id,
        p_content,
        p_message_type,
        p_related_item_id,
        v_user_is_p1,  -- 如果當前用戶是 p1,則 p1 已讀
        NOT v_user_is_p1,  -- 如果當前用戶是 p2,則 p2 已讀
        CASE WHEN v_user_is_p1 THEN now() ELSE NULL END,
        CASE WHEN v_user_is_p1 THEN NULL ELSE now() END
    )
    RETURNING id INTO v_message_id;

    -- 如果訊息關聯了商品,加入到 conversation_items_v2
    IF p_related_item_id IS NOT NULL THEN
        INSERT INTO public.conversation_items_v2 (
            conversation_id,
            item_id,
            added_by_user_id,
            added_via_message_id
        ) VALUES (
            p_conversation_id,
            p_related_item_id,
            v_current_user_id,
            v_message_id
        )
        ON CONFLICT (conversation_id, item_id) DO NOTHING;
    END IF;

    -- 返回訊息資訊
    RETURN QUERY
    SELECT
        m.id,
        m.conversation_id,
        m.sender_id,
        m.content,
        m.message_type,
        m.related_item_id,
        m.created_at
    FROM public.conversation_messages_v2 m
    WHERE m.id = v_message_id;
END;
$$;

COMMENT ON FUNCTION send_message_v2 IS 'v2: 發送訊息,支援商品引用和自動管理已讀狀態';
```

### 4. 查詢對話列表 v2

```sql
-- ============================================
-- 查詢用戶對話列表 v2
-- ============================================
CREATE OR REPLACE FUNCTION get_user_conversations_v2(
    p_page INT DEFAULT 1,
    p_size INT DEFAULT 20,
    p_include_archived BOOLEAN DEFAULT false
)
RETURNS TABLE(
    conversation_id BIGINT,
    other_user_id UUID,
    other_user_name VARCHAR,
    other_user_avatar TEXT,
    initial_item_id BIGINT,
    initial_item_title VARCHAR,
    last_message_content TEXT,
    last_message_at TIMESTAMPTZ,
    unread_count BIGINT,
    is_archived BOOLEAN,
    created_at TIMESTAMPTZ
)
LANGUAGE plpgsql
SECURITY DEFINER
STABLE
AS $$
DECLARE
    v_current_user_id UUID;
    v_offset INT;
BEGIN
    -- 取得當前用戶
    v_current_user_id := auth.uid();

    IF v_current_user_id IS NULL THEN
        RAISE EXCEPTION 'Not authenticated';
    END IF;

    v_offset := (p_page - 1) * p_size;

    RETURN QUERY
    SELECT
        c.id AS conversation_id,
        -- 對方用戶 ID (根據當前用戶是 p1 還是 p2 來決定)
        CASE
            WHEN c.participant_1_id = v_current_user_id THEN c.participant_2_id
            ELSE c.participant_1_id
        END AS other_user_id,
        -- 對方用戶資訊
        CASE
            WHEN c.participant_1_id = v_current_user_id THEN u2.name
            ELSE u1.name
        END AS other_user_name,
        CASE
            WHEN c.participant_1_id = v_current_user_id THEN u2.avatar_url
            ELSE u1.avatar_url
        END AS other_user_avatar,
        -- 初始商品資訊
        c.initial_item_id,
        i.title AS initial_item_title,
        -- 最後訊息
        (
            SELECT m.content
            FROM public.conversation_messages_v2 m
            WHERE m.conversation_id = c.id
              AND m.is_deleted = false
            ORDER BY m.created_at DESC
            LIMIT 1
        ) AS last_message_content,
        c.last_message_at,
        -- 未讀數量 (根據當前用戶是哪個參與者)
        (
            SELECT COUNT(*)
            FROM public.conversation_messages_v2 m
            WHERE m.conversation_id = c.id
              AND m.is_deleted = false
              AND m.sender_id != v_current_user_id
              AND (
                  (c.participant_1_id = v_current_user_id AND m.read_by_participant_1 = false)
                  OR
                  (c.participant_2_id = v_current_user_id AND m.read_by_participant_2 = false)
              )
        ) AS unread_count,
        -- 是否歸檔
        CASE
            WHEN c.participant_1_id = v_current_user_id THEN c.archived_by_participant_1
            ELSE c.archived_by_participant_2
        END AS is_archived,
        c.created_at
    FROM public.conversations_v2 c
    LEFT JOIN public.users u1 ON u1.id = c.participant_1_id
    LEFT JOIN public.users u2 ON u2.id = c.participant_2_id
    LEFT JOIN public.items i ON i.id = c.initial_item_id
    WHERE
        (c.participant_1_id = v_current_user_id OR c.participant_2_id = v_current_user_id)
        AND c.is_active = true
        AND (
            p_include_archived = true
            OR
            (
                (c.participant_1_id = v_current_user_id AND c.archived_by_participant_1 = false)
                OR
                (c.participant_2_id = v_current_user_id AND c.archived_by_participant_2 = false)
            )
        )
    ORDER BY c.last_message_at DESC NULLS LAST, c.created_at DESC
    LIMIT p_size
    OFFSET v_offset;
END;
$$;

COMMENT ON FUNCTION get_user_conversations_v2 IS 'v2: 查詢用戶對話列表,自動判斷對方用戶和未讀狀態';
```

### 5. 查詢對話訊息 v2

```sql
-- ============================================
-- 查詢對話訊息 v2
-- ============================================
CREATE OR REPLACE FUNCTION get_conversation_messages_v2(
    p_conversation_id BIGINT,
    p_page INT DEFAULT 1,
    p_size INT DEFAULT 50,
    p_include_deleted BOOLEAN DEFAULT false
)
RETURNS TABLE(
    message_id BIGINT,
    sender_id UUID,
    sender_name VARCHAR,
    sender_avatar TEXT,
    content TEXT,
    message_type VARCHAR,
    related_item_id BIGINT,
    related_item_title VARCHAR,
    is_deleted BOOLEAN,
    is_mine BOOLEAN,
    is_read BOOLEAN,
    created_at TIMESTAMPTZ
)
LANGUAGE plpgsql
SECURITY DEFINER
STABLE
AS $$
DECLARE
    v_current_user_id UUID;
    v_offset INT;
    v_participant_1 UUID;
    v_participant_2 UUID;
    v_user_is_p1 BOOLEAN;
BEGIN
    -- 取得當前用戶
    v_current_user_id := auth.uid();

    IF v_current_user_id IS NULL THEN
        RAISE EXCEPTION 'Not authenticated';
    END IF;

    -- 驗證用戶是對話參與者
    SELECT c.participant_1_id, c.participant_2_id
    INTO v_participant_1, v_participant_2
    FROM public.conversations_v2 c
    WHERE c.id = p_conversation_id;

    IF v_participant_1 IS NULL THEN
        RAISE EXCEPTION 'Conversation not found';
    END IF;

    IF v_current_user_id != v_participant_1 AND v_current_user_id != v_participant_2 THEN
        RAISE EXCEPTION 'Not a participant of this conversation';
    END IF;

    v_user_is_p1 := (v_current_user_id = v_participant_1);
    v_offset := (p_page - 1) * p_size;

    RETURN QUERY
    SELECT
        m.id AS message_id,
        m.sender_id,
        u.name AS sender_name,
        u.avatar_url AS sender_avatar,
        m.content,
        m.message_type,
        m.related_item_id,
        i.title AS related_item_title,
        m.is_deleted,
        (m.sender_id = v_current_user_id) AS is_mine,
        -- 已讀狀態 (根據當前用戶是哪個參與者)
        CASE
            WHEN v_user_is_p1 THEN m.read_by_participant_1
            ELSE m.read_by_participant_2
        END AS is_read,
        m.created_at
    FROM public.conversation_messages_v2 m
    LEFT JOIN public.users u ON u.id = m.sender_id
    LEFT JOIN public.items i ON i.id = m.related_item_id
    WHERE
        m.conversation_id = p_conversation_id
        AND (p_include_deleted = true OR m.is_deleted = false)
    ORDER BY m.created_at DESC
    LIMIT p_size
    OFFSET v_offset;
END;
$$;

COMMENT ON FUNCTION get_conversation_messages_v2 IS 'v2: 查詢對話訊息,包含商品引用資訊';
```

### 6. 標記訊息為已讀 v2

```sql
-- ============================================
-- 標記訊息為已讀 v2
-- ============================================
CREATE OR REPLACE FUNCTION mark_messages_as_read_v2(
    p_conversation_id BIGINT,
    p_up_to_message_id BIGINT DEFAULT NULL
)
RETURNS BIGINT
LANGUAGE plpgsql
SECURITY DEFINER
AS $$
DECLARE
    v_current_user_id UUID;
    v_participant_1 UUID;
    v_participant_2 UUID;
    v_user_is_p1 BOOLEAN;
    v_updated_count BIGINT;
BEGIN
    -- 取得當前用戶
    v_current_user_id := auth.uid();

    IF v_current_user_id IS NULL THEN
        RAISE EXCEPTION 'Not authenticated';
    END IF;

    -- 驗證用戶是對話參與者
    SELECT c.participant_1_id, c.participant_2_id
    INTO v_participant_1, v_participant_2
    FROM public.conversations_v2 c
    WHERE c.id = p_conversation_id;

    IF v_participant_1 IS NULL THEN
        RAISE EXCEPTION 'Conversation not found';
    END IF;

    IF v_current_user_id != v_participant_1 AND v_current_user_id != v_participant_2 THEN
        RAISE EXCEPTION 'Not a participant of this conversation';
    END IF;

    v_user_is_p1 := (v_current_user_id = v_participant_1);

    -- 更新已讀狀態 (只標記對方發送的訊息)
    IF v_user_is_p1 THEN
        UPDATE public.conversation_messages_v2
        SET
            read_by_participant_1 = true,
            read_at_participant_1 = now()
        WHERE
            conversation_id = p_conversation_id
            AND sender_id != v_current_user_id
            AND read_by_participant_1 = false
            AND (p_up_to_message_id IS NULL OR id <= p_up_to_message_id);
    ELSE
        UPDATE public.conversation_messages_v2
        SET
            read_by_participant_2 = true,
            read_at_participant_2 = now()
        WHERE
            conversation_id = p_conversation_id
            AND sender_id != v_current_user_id
            AND read_by_participant_2 = false
            AND (p_up_to_message_id IS NULL OR id <= p_up_to_message_id);
    END IF;

    GET DIAGNOSTICS v_updated_count = ROW_COUNT;

    RETURN v_updated_count;
END;
$$;

COMMENT ON FUNCTION mark_messages_as_read_v2 IS 'v2: 標記訊息為已讀,自動判斷當前用戶是哪個參與者';
```

### 7. 查詢對話中的所有商品 v2

```sql
-- ============================================
-- 查詢對話中討論的所有商品 v2
-- ============================================
CREATE OR REPLACE FUNCTION get_conversation_items_v2(
    p_conversation_id BIGINT
)
RETURNS TABLE(
    item_id BIGINT,
    item_title VARCHAR,
    item_price NUMERIC,
    item_image_url TEXT,
    item_status VARCHAR,
    added_by_user_id UUID,
    added_by_user_name VARCHAR,
    added_at TIMESTAMPTZ,
    message_count BIGINT
)
LANGUAGE plpgsql
SECURITY DEFINER
STABLE
AS $$
DECLARE
    v_current_user_id UUID;
BEGIN
    -- 取得當前用戶
    v_current_user_id := auth.uid();

    IF v_current_user_id IS NULL THEN
        RAISE EXCEPTION 'Not authenticated';
    END IF;

    -- 驗證用戶是對話參與者
    IF NOT EXISTS (
        SELECT 1 FROM public.conversations_v2 c
        WHERE c.id = p_conversation_id
          AND (c.participant_1_id = v_current_user_id OR c.participant_2_id = v_current_user_id)
    ) THEN
        RAISE EXCEPTION 'Not a participant of this conversation';
    END IF;

    RETURN QUERY
    SELECT
        ci.item_id,
        i.title AS item_title,
        i.price AS item_price,
        i.image_url AS item_image_url,
        i.status AS item_status,
        ci.added_by_user_id,
        u.name AS added_by_user_name,
        ci.created_at AS added_at,
        -- 討論此商品的訊息數量
        (
            SELECT COUNT(*)
            FROM public.conversation_messages_v2 m
            WHERE m.conversation_id = p_conversation_id
              AND m.related_item_id = ci.item_id
              AND m.is_deleted = false
        ) AS message_count
    FROM public.conversation_items_v2 ci
    LEFT JOIN public.items i ON i.id = ci.item_id
    LEFT JOIN public.users u ON u.id = ci.added_by_user_id
    WHERE
        ci.conversation_id = p_conversation_id
        AND ci.is_active = true
    ORDER BY ci.created_at ASC;
END;
$$;

COMMENT ON FUNCTION get_conversation_items_v2 IS 'v2: 查詢對話中討論過的所有商品';
```

### 8. 歸檔/取消歸檔對話 v2

```sql
-- ============================================
-- 歸檔或取消歸檔對話 v2
-- ============================================
CREATE OR REPLACE FUNCTION toggle_conversation_archive_v2(
    p_conversation_id BIGINT,
    p_archived BOOLEAN
)
RETURNS BOOLEAN
LANGUAGE plpgsql
SECURITY DEFINER
AS $$
DECLARE
    v_current_user_id UUID;
    v_participant_1 UUID;
    v_participant_2 UUID;
BEGIN
    -- 取得當前用戶
    v_current_user_id := auth.uid();

    IF v_current_user_id IS NULL THEN
        RAISE EXCEPTION 'Not authenticated';
    END IF;

    -- 驗證用戶是對話參與者
    SELECT c.participant_1_id, c.participant_2_id
    INTO v_participant_1, v_participant_2
    FROM public.conversations_v2 c
    WHERE c.id = p_conversation_id;

    IF v_participant_1 IS NULL THEN
        RAISE EXCEPTION 'Conversation not found';
    END IF;

    IF v_current_user_id != v_participant_1 AND v_current_user_id != v_participant_2 THEN
        RAISE EXCEPTION 'Not a participant of this conversation';
    END IF;

    -- 更新歸檔狀態
    IF v_current_user_id = v_participant_1 THEN
        UPDATE public.conversations_v2
        SET archived_by_participant_1 = p_archived
        WHERE id = p_conversation_id;
    ELSE
        UPDATE public.conversations_v2
        SET archived_by_participant_2 = p_archived
        WHERE id = p_conversation_id;
    END IF;

    RETURN true;
END;
$$;

COMMENT ON FUNCTION toggle_conversation_archive_v2 IS 'v2: 歸檔或取消歸檔對話';
```

---

## 🔄 數據遷移策略

### 遷移輔助函數

```sql
-- ============================================
-- v1 到 v2 數據遷移函數
-- ============================================
CREATE OR REPLACE FUNCTION migrate_conversations_v1_to_v2(
    p_batch_size INT DEFAULT 1000,
    p_offset INT DEFAULT 0
)
RETURNS TABLE(
    migrated_conversations BIGINT,
    migrated_messages BIGINT,
    errors TEXT[]
)
LANGUAGE plpgsql
SECURITY DEFINER
AS $$
DECLARE
    v_migrated_conv BIGINT := 0;
    v_migrated_msg BIGINT := 0;
    v_errors TEXT[] := ARRAY[]::TEXT[];
    v_conv_record RECORD;
    v_new_conv_id BIGINT;
    v_participant_1 UUID;
    v_participant_2 UUID;
BEGIN
    -- 遷移對話
    FOR v_conv_record IN
        SELECT * FROM public.conversations
        ORDER BY id
        LIMIT p_batch_size
        OFFSET p_offset
    LOOP
        BEGIN
            -- 標準化參與者順序
            SELECT p1, p2 INTO v_participant_1, v_participant_2
            FROM normalize_participants_v2(
                v_conv_record.buyer_id,
                v_conv_record.seller_id
            ) AS t(p1, p2);

            -- 插入到 v2 (或取得已存在的對話)
            INSERT INTO public.conversations_v2 (
                participant_1_id,
                participant_2_id,
                initial_item_id,
                created_at,
                updated_at
            ) VALUES (
                v_participant_1,
                v_participant_2,
                v_conv_record.item_id,
                v_conv_record.created_at,
                v_conv_record.updated_at
            )
            ON CONFLICT (participant_1_id, participant_2_id)
            DO UPDATE SET updated_at = EXCLUDED.updated_at
            RETURNING id INTO v_new_conv_id;

            -- 確保初始商品在 conversation_items_v2 中
            INSERT INTO public.conversation_items_v2 (
                conversation_id,
                item_id,
                added_by_user_id,
                created_at
            ) VALUES (
                v_new_conv_id,
                v_conv_record.item_id,
                v_conv_record.buyer_id,  -- 假設買家發起
                v_conv_record.created_at
            )
            ON CONFLICT (conversation_id, item_id) DO NOTHING;

            v_migrated_conv := v_migrated_conv + 1;

            -- 遷移此對話的所有訊息
            WITH migrated AS (
                INSERT INTO public.conversation_messages_v2 (
                    conversation_id,
                    sender_id,
                    content,
                    message_type,
                    is_deleted,
                    read_by_participant_1,
                    read_by_participant_2,
                    created_at,
                    updated_at
                )
                SELECT
                    v_new_conv_id,
                    cm.sender_id,
                    cm.content,
                    cm.message_type,
                    cm.is_deleted,
                    -- 根據發送者和接收者設定已讀狀態
                    CASE
                        WHEN cm.sender_id = v_participant_1 THEN true
                        WHEN cm.receiver_id = v_participant_1 THEN cm.is_read
                        ELSE false
                    END,
                    CASE
                        WHEN cm.sender_id = v_participant_2 THEN true
                        WHEN cm.receiver_id = v_participant_2 THEN cm.is_read
                        ELSE false
                    END,
                    cm.created_at,
                    cm.updated_at
                FROM public.conversation_messages cm
                WHERE cm.conversation_id = v_conv_record.id
                ON CONFLICT DO NOTHING
                RETURNING 1
            )
            SELECT COUNT(*) INTO v_migrated_msg
            FROM migrated;

        EXCEPTION WHEN OTHERS THEN
            v_errors := array_append(
                v_errors,
                format('Error migrating conversation %s: %s', v_conv_record.id, SQLERRM)
            );
        END;
    END LOOP;

    RETURN QUERY SELECT v_migrated_conv, v_migrated_msg, v_errors;
END;
$$;

COMMENT ON FUNCTION migrate_conversations_v1_to_v2 IS '批次遷移 v1 對話和訊息到 v2';
```

### 遷移驗證函數

```sql
-- ============================================
-- 驗證遷移完整性
-- ============================================
CREATE OR REPLACE FUNCTION verify_migration_v1_to_v2()
RETURNS TABLE(
    check_name TEXT,
    v1_count BIGINT,
    v2_count BIGINT,
    is_valid BOOLEAN,
    message TEXT
)
LANGUAGE plpgsql
SECURITY DEFINER
AS $$
BEGIN
    -- 檢查 1: 對話數量
    RETURN QUERY
    SELECT
        'Conversation Count'::TEXT,
        (SELECT COUNT(*) FROM public.conversations)::BIGINT,
        (SELECT COUNT(*) FROM public.conversations_v2)::BIGINT,
        (SELECT COUNT(*) FROM public.conversations) <= (SELECT COUNT(*) FROM public.conversations_v2),
        'v2 should have equal or more conversations (due to deduplication)'::TEXT;

    -- 檢查 2: 訊息數量
    RETURN QUERY
    SELECT
        'Message Count'::TEXT,
        (SELECT COUNT(*) FROM public.conversation_messages)::BIGINT,
        (SELECT COUNT(*) FROM public.conversation_messages_v2)::BIGINT,
        (SELECT COUNT(*) FROM public.conversation_messages) = (SELECT COUNT(*) FROM public.conversation_messages_v2),
        'Message counts should match'::TEXT;

    -- 檢查 3: 參與者順序正確性
    RETURN QUERY
    SELECT
        'Participant Order'::TEXT,
        NULL::BIGINT,
        (SELECT COUNT(*) FROM public.conversations_v2 WHERE participant_1_id >= participant_2_id)::BIGINT,
        (SELECT COUNT(*) FROM public.conversations_v2 WHERE participant_1_id >= participant_2_id) = 0,
        'All conversations should have participant_1_id < participant_2_id'::TEXT;

    -- 檢查 4: 商品關聯
    RETURN QUERY
    SELECT
        'Item Associations'::TEXT,
        (SELECT COUNT(DISTINCT item_id) FROM public.conversations)::BIGINT,
        (SELECT COUNT(*) FROM public.conversation_items_v2)::BIGINT,
        (SELECT COUNT(*) FROM public.conversation_items_v2) >= (SELECT COUNT(DISTINCT item_id) FROM public.conversations),
        'v2 should track all items'::TEXT;
END;
$$;

COMMENT ON FUNCTION verify_migration_v1_to_v2 IS '驗證 v1 到 v2 遷移的完整性';
```

---

## 📱 前端整合範例

### TypeScript 類型定義

```typescript
// types/messaging-v2.ts

export interface ConversationV2 {
  conversation_id: number;
  other_user_id: string;
  other_user_name: string;
  other_user_avatar: string | null;
  initial_item_id: number | null;
  initial_item_title: string | null;
  last_message_content: string | null;
  last_message_at: string | null;
  unread_count: number;
  is_archived: boolean;
  created_at: string;
}

export interface MessageV2 {
  message_id: number;
  sender_id: string;
  sender_name: string;
  sender_avatar: string | null;
  content: string;
  message_type: 'text' | 'image' | 'system' | 'item_reference';
  related_item_id: number | null;
  related_item_title: string | null;
  is_deleted: boolean;
  is_mine: boolean;
  is_read: boolean;
  created_at: string;
}

export interface ConversationItemV2 {
  item_id: number;
  item_title: string;
  item_price: number;
  item_image_url: string | null;
  item_status: string;
  added_by_user_id: string;
  added_by_user_name: string;
  added_at: string;
  message_count: number;
}

export interface CreateConversationResult {
  conversation_id: number;
  participant_1_id: string;
  participant_2_id: string;
  initial_item_id: number | null;
  is_new: boolean;
  current_user_is_participant_1: boolean;
}
```

### API 封裝

```typescript
// lib/messaging-v2.ts

import { createClient } from '@supabase/supabase-js';

const supabase = createClient(process.env.SUPABASE_URL!, process.env.SUPABASE_ANON_KEY!);

export class MessagingV2 {
  /**
   * 建立或取得對話
   */
  static async createOrGetConversation(
    otherUserId: string,
    initialItemId?: number
  ): Promise<CreateConversationResult> {
    const { data, error } = await supabase.rpc('create_or_get_conversation_v2', {
      p_other_user_id: otherUserId,
      p_initial_item_id: initialItemId || null,
    });

    if (error) throw error;
    return data[0];
  }

  /**
   * 發送訊息
   */
  static async sendMessage(
    conversationId: number,
    content: string,
    options?: {
      messageType?: 'text' | 'image' | 'item_reference';
      relatedItemId?: number;
    }
  ): Promise<MessageV2> {
    const { data, error } = await supabase.rpc('send_message_v2', {
      p_conversation_id: conversationId,
      p_content: content,
      p_message_type: options?.messageType || 'text',
      p_related_item_id: options?.relatedItemId || null,
    });

    if (error) throw error;
    return data[0];
  }

  /**
   * 查詢對話列表
   */
  static async getConversations(
    page: number = 1,
    pageSize: number = 20,
    includeArchived: boolean = false
  ): Promise<ConversationV2[]> {
    const { data, error } = await supabase.rpc('get_user_conversations_v2', {
      p_page: page,
      p_size: pageSize,
      p_include_archived: includeArchived,
    });

    if (error) throw error;
    return data;
  }

  /**
   * 查詢對話訊息
   */
  static async getMessages(
    conversationId: number,
    page: number = 1,
    pageSize: number = 50
  ): Promise<MessageV2[]> {
    const { data, error } = await supabase.rpc('get_conversation_messages_v2', {
      p_conversation_id: conversationId,
      p_page: page,
      p_size: pageSize,
      p_include_deleted: false,
    });

    if (error) throw error;
    return data;
  }

  /**
   * 標記訊息為已讀
   */
  static async markAsRead(conversationId: number): Promise<number> {
    const { data, error } = await supabase.rpc('mark_messages_as_read_v2', {
      p_conversation_id: conversationId,
    });

    if (error) throw error;
    return data;
  }

  /**
   * 查詢對話中的商品
   */
  static async getConversationItems(
    conversationId: number
  ): Promise<ConversationItemV2[]> {
    const { data, error } = await supabase.rpc('get_conversation_items_v2', {
      p_conversation_id: conversationId,
    });

    if (error) throw error;
    return data;
  }

  /**
   * 歸檔對話
   */
  static async archiveConversation(
    conversationId: number,
    archived: boolean = true
  ): Promise<boolean> {
    const { data, error } = await supabase.rpc('toggle_conversation_archive_v2', {
      p_conversation_id: conversationId,
      p_archived: archived,
    });

    if (error) throw error;
    return data;
  }

  /**
   * Realtime 訂閱新訊息
   */
  static subscribeToMessages(
    conversationId: number,
    callback: (message: MessageV2) => void
  ) {
    return supabase
      .channel(`conversation_v2_${conversationId}`)
      .on(
        'postgres_changes',
        {
          event: 'INSERT',
          schema: 'public',
          table: 'conversation_messages_v2',
          filter: `conversation_id=eq.${conversationId}`,
        },
        async (payload) => {
          // 取得完整訊息資訊
          const { data } = await supabase.rpc('get_conversation_messages_v2', {
            p_conversation_id: conversationId,
            p_page: 1,
            p_size: 1,
          });

          if (data && data.length > 0) {
            callback(data[0]);
          }
        }
      )
      .subscribe();
  }
}
```

### React 組件範例

```typescript
// components/ConversationListV2.tsx

import { useEffect, useState } from 'react';
import { MessagingV2 } from '@/lib/messaging-v2';
import type { ConversationV2 } from '@/types/messaging-v2';

export function ConversationListV2() {
  const [conversations, setConversations] = useState<ConversationV2[]>([]);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    loadConversations();
  }, []);

  async function loadConversations() {
    try {
      const data = await MessagingV2.getConversations(1, 20);
      setConversations(data);
    } catch (error) {
      console.error('Failed to load conversations:', error);
    } finally {
      setLoading(false);
    }
  }

  if (loading) return <div>載入中...</div>;

  return (
    <div className="conversation-list">
      {conversations.map((conv) => (
        <div key={conv.conversation_id} className="conversation-item">
          <div className="user-info">
            <img src={conv.other_user_avatar || '/default-avatar.png'} alt="" />
            <div>
              <h3>{conv.other_user_name}</h3>
              {conv.initial_item_title && (
                <p className="item-title">關於: {conv.initial_item_title}</p>
              )}
            </div>
          </div>
          <div className="message-preview">
            <p>{conv.last_message_content}</p>
            {conv.unread_count > 0 && (
              <span className="badge">{conv.unread_count}</span>
            )}
          </div>
        </div>
      ))}
    </div>
  );
}
```

```typescript
// components/ChatRoomV2.tsx

import { useEffect, useState, useRef } from 'react';
import { MessagingV2 } from '@/lib/messaging-v2';
import type { MessageV2, ConversationItemV2 } from '@/types/messaging-v2';

interface Props {
  conversationId: number;
}

export function ChatRoomV2({ conversationId }: Props) {
  const [messages, setMessages] = useState<MessageV2[]>([]);
  const [items, setItems] = useState<ConversationItemV2[]>([]);
  const [inputText, setInputText] = useState('');
  const [selectedItemId, setSelectedItemId] = useState<number | null>(null);
  const subscriptionRef = useRef<any>(null);

  useEffect(() => {
    loadMessages();
    loadItems();
    subscribeToNewMessages();
    markAsRead();

    return () => {
      if (subscriptionRef.current) {
        subscriptionRef.current.unsubscribe();
      }
    };
  }, [conversationId]);

  async function loadMessages() {
    const data = await MessagingV2.getMessages(conversationId);
    setMessages(data.reverse()); // 反轉順序,最新在下
  }

  async function loadItems() {
    const data = await MessagingV2.getConversationItems(conversationId);
    setItems(data);
  }

  function subscribeToNewMessages() {
    subscriptionRef.current = MessagingV2.subscribeToMessages(
      conversationId,
      (newMessage) => {
        setMessages((prev) => [...prev, newMessage]);
        if (!newMessage.is_mine) {
          markAsRead();
        }
      }
    );
  }

  async function markAsRead() {
    await MessagingV2.markAsRead(conversationId);
  }

  async function sendMessage() {
    if (!inputText.trim()) return;

    try {
      await MessagingV2.sendMessage(conversationId, inputText, {
        messageType: selectedItemId ? 'item_reference' : 'text',
        relatedItemId: selectedItemId || undefined,
      });

      setInputText('');
      setSelectedItemId(null);
    } catch (error) {
      console.error('Failed to send message:', error);
    }
  }

  return (
    <div className="chat-room">
      {/* 商品列表區 */}
      {items.length > 0 && (
        <div className="conversation-items">
          <h4>討論中的商品 ({items.length})</h4>
          <div className="items-grid">
            {items.map((item) => (
              <div
                key={item.item_id}
                className={`item-card ${selectedItemId === item.item_id ? 'selected' : ''}`}
                onClick={() => setSelectedItemId(item.item_id)}
              >
                <img src={item.item_image_url || '/placeholder.png'} alt={item.item_title} />
                <div className="item-info">
                  <h5>{item.item_title}</h5>
                  <p className="price">${item.item_price}</p>
                  <p className="message-count">{item.message_count} 則相關訊息</p>
                </div>
              </div>
            ))}
          </div>
        </div>
      )}

      {/* 訊息列表區 */}
      <div className="messages">
        {messages.map((msg) => (
          <div
            key={msg.message_id}
            className={`message ${msg.is_mine ? 'mine' : 'other'}`}
          >
            <div className="message-content">
              <p>{msg.content}</p>
              {msg.related_item_id && (
                <div className="related-item-tag">
                  📦 {msg.related_item_title}
                </div>
              )}
            </div>
            <span className="timestamp">
              {new Date(msg.created_at).toLocaleTimeString()}
            </span>
          </div>
        ))}
      </div>

      {/* 輸入區 */}
      <div className="message-input">
        {selectedItemId && (
          <div className="selected-item-indicator">
            正在討論: {items.find((i) => i.item_id === selectedItemId)?.item_title}
            <button onClick={() => setSelectedItemId(null)}>✕</button>
          </div>
        )}
        <input
          type="text"
          value={inputText}
          onChange={(e) => setInputText(e.target.value)}
          onKeyPress={(e) => e.key === 'Enter' && sendMessage()}
          placeholder="輸入訊息..."
        />
        <button onClick={sendMessage}>發送</button>
      </div>
    </div>
  );
}
```

---

## 🚀 遷移實施計畫

### 階段 1: 準備階段 (1 週)

#### 1.1 建立 v2 資料表和函數

```bash
# 執行 SQL 腳本
psql -f migrations/create_messaging_v2_schema.sql
```

**檢查清單**:
- ✅ 建立 `conversations_v2` 表
- ✅ 建立 `conversation_messages_v2` 表
- ✅ 建立 `conversation_items_v2` 表
- ✅ 建立所有索引
- ✅ 設定 RLS 政策
- ✅ 建立所有 v2 RPC 函數
- ✅ 執行基本功能測試

### 階段 3: 測試階段 (1 週)

#### 3.1 單元測試

```sql
-- 測試建立對話
DO $$
DECLARE
    v_result RECORD;
    v_user_a UUID := gen_random_uuid();
    v_user_b UUID := gen_random_uuid();
BEGIN
    -- 測試 1: 建立新對話
    SELECT * INTO v_result
    FROM create_or_get_conversation_v2(v_user_b, 123);

    ASSERT v_result.is_new = true, 'Should create new conversation';

    -- 測試 2: 取得相同對話
    SELECT * INTO v_result
    FROM create_or_get_conversation_v2(v_user_b, 123);

    ASSERT v_result.is_new = false, 'Should return existing conversation';

    RAISE NOTICE 'All tests passed!';
END $$;
```

#### 3.2 效能測試

```sql
-- 測試查詢效能
EXPLAIN ANALYZE
SELECT * FROM get_user_conversations_v2(1, 20, false);

EXPLAIN ANALYZE
SELECT * FROM get_conversation_messages_v2(1, 1, 50, false);
```

### 階段 4: 漸進式部署 (2 週)

#### 4.1 功能開關

```typescript
// config/features.ts
export const FEATURES = {
  USE_MESSAGING_V2: process.env.NEXT_PUBLIC_USE_MESSAGING_V2 === 'true',
  MESSAGING_V2_ROLLOUT_PERCENTAGE: parseInt(
    process.env.NEXT_PUBLIC_MESSAGING_V2_ROLLOUT || '0'
  ),
};

// lib/messaging-adapter.ts
import { MessagingV1 } from './messaging-v1';
import { MessagingV2 } from './messaging-v2';
import { FEATURES } from '@/config/features';

export class MessagingAdapter {
  private static shouldUseV2(userId: string): boolean {
    if (!FEATURES.USE_MESSAGING_V2) return false;

    // 基於用戶 ID 的漸進式推出
    const hash = userId.split('').reduce((acc, char) =>
      acc + char.charCodeAt(0), 0
    );
    const percentage = hash % 100;

    return percentage < FEATURES.MESSAGING_V2_ROLLOUT_PERCENTAGE;
  }

  static async createConversation(
    userId: string,
    otherUserId: string,
    itemId?: number
  ) {
    if (this.shouldUseV2(userId)) {
      return MessagingV2.createOrGetConversation(otherUserId, itemId);
    } else {
      return MessagingV1.createOrGetConversation(otherUserId, itemId);
    }
  }

  // ... 其他方法類似
}
```

#### 4.2 推出時程

| 週次 | v2 使用比例 | 說明 |
|------|-------------|------|
| 第 1 週 | 5% | 金絲雀部署,監控錯誤率 |
| 第 2 週 | 20% | 擴大測試範圍 |
| 第 3 週 | 50% | 半數用戶使用 v2 |
| 第 4 週 | 100% | 全面切換到 v2 |

### 階段 5: 清理階段 (未來)

**注意**: 僅在 v2 穩定運行 3-6 個月後執行

```sql
-- 標記 v1 表為棄用 (不要立即刪除)
COMMENT ON TABLE public.conversations IS 'DEPRECATED: Use conversations_v2 instead';
COMMENT ON TABLE public.conversation_messages IS 'DEPRECATED: Use conversation_messages_v2 instead';

-- 移除 v1 RPC 函數
DROP FUNCTION IF EXISTS create_or_get_conversation CASCADE;
DROP FUNCTION IF EXISTS send_message CASCADE;
-- ... 其他 v1 函數

-- 最後,在確認無任何依賴後,刪除 v1 表
-- DROP TABLE IF EXISTS public.conversation_messages CASCADE;
-- DROP TABLE IF EXISTS public.conversations CASCADE;
```

---

## 📊 v1 vs v2 對照表

### 功能對照

| 功能 | v1 | v2 | 備註 |
|------|----|----|------|
| 建立對話 | `create_or_get_conversation()` | `create_or_get_conversation_v2()` | v2 支援去角色化 |
| 查詢對話列表 | `get_user_conversations()` | `get_user_conversations_v2()` | v2 自動判斷對方用戶 |
| 發送訊息 | `send_message()` | `send_message_v2()` | v2 支援商品引用 |
| 標記已讀 | `mark_messages_as_read()` | `mark_messages_as_read_v2()` | v2 自動判斷參與者 |
| 查詢訊息 | `get_conversation_messages()` | `get_conversation_messages_v2()` | v2 包含商品資訊 |
| 商品關聯 | 單一商品 (固定) | 多商品 (動態) | v2 新增功能 |
| 歸檔對話 | `soft_delete_conversation()` | `toggle_conversation_archive_v2()` | v2 改用歸檔概念 |

### 資料表結構對照

| v1 欄位 | v2 欄位 | 變更說明 |
|---------|---------|----------|
| `buyer_id` | `participant_1_id` | 去角色化 |
| `seller_id` | `participant_2_id` | 去角色化 |
| `item_id` | `initial_item_id` | 改為可選,初始商品 |
| - | `archived_by_participant_1` | 新增:個別歸檔狀態 |
| - | `archived_by_participant_2` | 新增:個別歸檔狀態 |
| - | `last_message_at` | 新增:快速排序 |
| `receiver_id` | - | 移除:改用參與者模型 |
| `is_read` | `read_by_participant_1` | 拆分為兩個欄位 |
| - | `read_by_participant_2` | 新增:個別已讀狀態 |
| - | `related_item_id` | 新增:訊息關聯商品 |

---

## 🎯 最佳實踐建議

### 1. 開發者指南

**使用 v2 API 時**:

```typescript
// ✅ 推薦: 使用 v2 API
import { MessagingV2 } from '@/lib/messaging-v2';

async function handleStartChat(otherUserId: string, itemId: number) {
  const conversation = await MessagingV2.createOrGetConversation(
    otherUserId,
    itemId
  );

  // v2 自動處理角色,無需擔心誰是買家賣家
  router.push(`/messages/${conversation.conversation_id}`);
}

// ❌ 避免: 直接操作 v2 表
// const { data } = await supabase.from('conversations_v2').insert(...);
```

### 2. 錯誤處理

```typescript
try {
  await MessagingV2.sendMessage(conversationId, content);
} catch (error) {
  if (error.message.includes('Not a participant')) {
    // 處理權限錯誤
    showError('您不是此對話的參與者');
  } else if (error.message.includes('Conversation not found')) {
    // 處理對話不存在
    showError('對話不存在');
  } else {
    // 其他錯誤
    showError('發送失敗,請稍後再試');
  }
}
```

### 3. 效能優化

```typescript
// ✅ 使用分頁避免一次載入過多資料
const conversations = await MessagingV2.getConversations(1, 20);

// ✅ 使用 Realtime 訂閱減少輪詢
const subscription = MessagingV2.subscribeToMessages(
  conversationId,
  handleNewMessage
);

// ✅ 及時取消訂閱
useEffect(() => {
  return () => subscription.unsubscribe();
}, []);
```

### 4. 安全性

```typescript
// ✅ 永遠通過 RPC 函數操作資料
await MessagingV2.sendMessage(conversationId, content);

// ❌ 避免直接插入資料 (會繞過 RLS 和驗證邏輯)
// await supabase.from('conversation_messages_v2').insert({ ... });

// ✅ 驗證用戶輸入
function sanitizeContent(content: string): string {
  return content.trim().slice(0, 5000); // 限制長度
}
```

---

## 📈 監控指標

### 關鍵指標

```sql
-- 建立監控視圖
CREATE OR REPLACE VIEW messaging_v2_metrics AS
SELECT
    'Total Conversations' as metric,
    COUNT(*)::TEXT as value
FROM public.conversations_v2
UNION ALL
SELECT
    'Active Conversations (Last 7 Days)',
    COUNT(*)::TEXT
FROM public.conversations_v2
WHERE last_message_at > now() - INTERVAL '7 days'
UNION ALL
SELECT
    'Total Messages',
    COUNT(*)::TEXT
FROM public.conversation_messages_v2
WHERE is_deleted = false
UNION ALL
SELECT
    'Messages Today',
    COUNT(*)::TEXT
FROM public.conversation_messages_v2
WHERE created_at > CURRENT_DATE
UNION ALL
SELECT
    'Average Messages per Conversation',
    ROUND(AVG(msg_count), 2)::TEXT
FROM (
    SELECT conversation_id, COUNT(*) as msg_count
    FROM public.conversation_messages_v2
    WHERE is_deleted = false
    GROUP BY conversation_id
) sub
UNION ALL
SELECT
    'Average Response Time (minutes)',
    ROUND(AVG(response_time_minutes), 2)::TEXT
FROM (
    SELECT
        m1.conversation_id,
        EXTRACT(EPOCH FROM (m2.created_at - m1.created_at)) / 60 as response_time_minutes
    FROM public.conversation_messages_v2 m1
    JOIN public.conversation_messages_v2 m2
        ON m1.conversation_id = m2.conversation_id
        AND m2.id = (
            SELECT MIN(id)
            FROM public.conversation_messages_v2
            WHERE conversation_id = m1.conversation_id
            AND id > m1.id
            AND sender_id != m1.sender_id
        )
    WHERE m1.created_at > now() - INTERVAL '7 days'
) response_times;

-- 查詢監控指標
SELECT * FROM messaging_v2_metrics;
```

---

## ❓ 常見問題 (FAQ)

### Q1: v1 和 v2 可以同時使用嗎?

**答**: 可以。v1 和 v2 是完全獨立的系統,可以並存。建議使用功能開關逐步切換用戶到 v2。

### Q2: 如果遷移過程中發現問題怎麼辦?

**答**:
1. v1 資料表完全不受影響,可以立即回退
2. 所有遷移操作都有記錄在 `migration_progress` 表
3. 可以重新執行遷移函數修復問題

### Q3: v2 的效能會比 v1 差嗎?

**答**: 不會。v2 經過優化:
- 更好的索引設計
- 減少 JOIN 操作
- 使用 CTE 優化複雜查詢

### Q4: 已讀狀態遷移如何處理?

**答**: 遷移函數會根據 v1 的 `sender_id` 和 `receiver_id` 自動映射到 v2 的 `read_by_participant_1/2`。

### Q5: 如果兩個對話在 v1 中因為角色不同而分開,v2 會合併嗎?

**答**: 會。遷移函數會自動合併同一對用戶的對話,並保留所有訊息。這正是 v2 的優勢之一。

### Q6: v2 支援群組對話嗎?

**答**: 目前 v2 仍是兩人對話設計。如需群組功能,需要額外的表結構設計。

---

## 📝 總結

### v2 核心優勢

1. **✅ 去角色化設計**: 買賣雙方可以自由切換角色
2. **✅ 多商品支援**: 一個對話可以討論多個商品
3. **✅ 更靈活的已讀狀態**: 獨立追蹤每個參與者的已讀狀態
4. **✅ 更好的歸檔機制**: 每個參與者可以獨立歸檔對話
5. **✅ 向後相容**: v1 系統不受影響,平滑過渡
6. **✅ 可擴展性**: 為未來功能預留空間

### 遷移建議時程

- **準備階段**: 1 週
- **測試階段**: 1 週
- **漸進式部署**: 2 週
- **監控期**: 持續

### 下一步行動

1. ✅ 建立 v2 資料表和函數
2. ✅ 執行數據遷移
3. ✅ 更新前端代碼支援 v2 API
4. ✅ 設定功能開關
5. ✅ 漸進式推出給用戶
6. ✅ 監控和優化

---

## 📚 相關文件

- [v1 用戶通訊模組架構說明](./用戶通訊模組架構說明.md)
- [v1 改造方案](./角色互換改造方案.md)
- [Supabase RLS 文檔](https://supabase.com/docs/guides/auth/row-level-security)
- [PostgreSQL 效能優化](https://www.postgresql.org/docs/current/performance-tips.html)

---

**文件版本**: 2.0.0
**最後更新**: 2024-01-XX
**維護者**: Development Team
